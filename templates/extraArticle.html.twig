<article class="col-block popular__post">
    <a href="{{ path('view_post', {'slug': post.slug}) }}" class="popular__thumb">
        <img src="{{ post.headerImage }}" alt="">
    </a>
    <h5><a href="{{ path('view_post', {'slug': post.slug}) }}">{{ post.title }}</a></h5>
    <section class="popular__meta">
        <span class="popular__author"><span>By</span> {{ post.author.username }} </span>
        <span class="popular__date"><span>on</span> <time datetime="{{ post.publishedAt|date("Y-m-d") }}">
                {{ post.publishedAt | date("j F, Y") }}</time></span>
    </section>
</article>